# NexAuto symbol

`nexauto-mark.png`: standalone N symbol, transparent PNG for light backgrounds. Created with the built-in image generation tool, using `nexauto-logo.png` as the edit target.

## Edit prompt

Edit target: the supplied NexAuto logo image. Extract only the existing abstract N symbol on the left, removing the entire NexAuto wordmark. Preserve the original N silhouette, geometry, proportions, curved interlocking strokes, negative spaces and near-black color exactly; do not redesign or reinterpret the mark. Present the N alone, centered on a square canvas with balanced modest padding (mark occupies about 75% width). Actual transparent alpha background, no white background, no new text, no shadows, no additional elements. Crisp high-resolution logo asset.
